#!/bin/bash
set -euo pipefail

step="$(basename "$0")"
printf '{"valid":true,"step":"%s","arguments":[' "$step"
separator=""
for argument in "$@"; do
  printf '%s"%s"' "$separator" "$argument"
  separator=","
done
printf ']}\n'
